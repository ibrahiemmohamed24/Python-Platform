[build-system]
requires = ["setuptools>=80", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-python-qa-platform"
version = "0.1.0"
description = "AI-powered Python Quality Assurance Platform"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools]
packages = ["app"]